pytest
numpy

# Web simulation dashboard (Flask - legacy)
flask
flask-socketio
python-engineio>=4.0.0
python-socketio>=5.0.0
# eventlet  # Optional, deprecated - threading mode works fine for simulation

# FastAPI (recommended for new deployments)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.5.0

# LLM integration
requests>=2.31.0

# MAVLink — talk to ArduPilot/PX4 (SITL or real flight controllers)
pymavlink>=2.4.0

# Optional: Local LLM with transformers (uncomment if using TransformersBackend)
# transformers>=4.36.0
# torch>=2.1.0
# accelerate>=0.25.0

# Optional: OpenAI API (uncomment if using OpenAIBackend)
# openai>=1.6.0
